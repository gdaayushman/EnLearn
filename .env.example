# Copy to .env — defaults work with the bundled docker-compose Postgres.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/edtech_pw?schema=public"
JWT_SECRET="change-me-to-a-long-random-string-at-least-32-chars"
JWT_EXPIRES_IN="7d"

# Optional file/video hosts
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Contact-to-buy: your Instagram handle is set in src/lib/config.ts
